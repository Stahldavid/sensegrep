---
"@sensegrep/core": minor
"@sensegrep/cli": minor
"@sensegrep/mcp": minor
---

Improve code discovery with a larger candidate pool, complete lexical file coverage, discriminative term scoring, deterministic reconstruction ordering, and parallel hybrid retrieval by default. Select compact relevant context before truncating oversized symbols.

Classify JavaScript/TypeScript graph calls from AST call sites, resolve named relative import aliases, and invalidate older graph caches. Prefer symbol-based survey/cluster titles over generic imports.

Detect exact duplicates before neighbor search, compare small eligible sets in memory, filter larger ANN queries before top-k, and persist snapshot-bound continuation checkpoints so resumed scans retain previous pairs.
